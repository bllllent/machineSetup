# Copy to .env (setup.sh does this for you, prompting for the token).
# .env is gitignored — never commit it (this repo is public).
# Token: Cloudflare dashboard -> My Profile -> API Tokens -> Create Token ->
# "Edit zone DNS" template, scoped to amokamok.com, PLUS Zone -> Zone -> Read.
CLOUDFLARE_API_TOKEN=changeme
